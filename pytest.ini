[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    acceptance_gap: executable requirement that is not implemented yet (strict xfail)
    postgres: integration test against disposable loopback PostgreSQL
